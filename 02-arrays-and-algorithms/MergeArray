#include <stdio.h>

int main() {
    int arr1[] = {1,2,3,4,5};
    int arr2[]={6,7,8,9};
    int s1 = 5;
    int s2 = 4;
    int s = s1 + s2;
    int arr[s];
    for(int i = 0 ; i < s1 ; i++){
            arr[i] = arr1[i];

    }
    for(int i = s1 ; i < s ; i++){
         
            arr[i] = arr2[i-s1];
    }
    
    for(int i = 0 ; i < s; i++){
     printf("%d ",arr[i]);
    }

    return 0;
}
